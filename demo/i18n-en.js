// The dashboard bundles 40+ locales. The preview only ever renders English, so
// this keeps the demo bundle small enough to commit.
import en from '../app/javascript/dashboard/i18n/locale/en';

export default { en };
